# Verified game loop and chose top-down

The learner built and ran the generated MonoGame project, correctly explained that `Update` changes game state before `Draw` presents it, and chose a top-down first prototype. They also corrected the verification procedure: `dotnet build` compiles, while `dotnet run` launches the game; future lessons should use both checks precisely.
